services:
  - type: web
    name: dota2-bot-django
    runtime: python
    plan: free
    # 👇 ЭТА СТРОКА ДОЛЖНА БЫТЬ (команда сборки)
    buildCommand: |
      pip install -r requirements.txt
      python manage.py migrate           # <-- миграции здесь
      python manage.py collectstatic --noinput
      python manage.py ensure_admin
    # 👇 ЭТА СТРОКА ДОЛЖНА БЫТЬ (команда запуска)
    startCommand: gunicorn dota2_bot_project.wsgi:application --bind 0.0.0.0:$PORT
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: dota2-bot-database
          property: connectionString
      - key: TELEGRAM_BOT_TOKEN
        sync: false
      - key: SECRET_KEY
        generateValue: true
      - key: DEBUG
        value: false
      - key: ALLOWED_HOSTS
        value: .onrender.com

databases:
  - name: dota2-bot-database
    plan: free